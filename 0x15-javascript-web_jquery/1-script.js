/** Write a JavaScript script that updates the text color of the <header> element to red (#FF0000) */

// Use jQuery to change the text color to red
$('1-main.html').ready(function() {
    $("header").css("color", "#FF0000");
});
